import React from 'react';
import './styles/Home.css';
import PropTypes from 'prop-types';

function ContactWidget(){

  return (
    <div className="contactWidget container">
      <p>Contact Widget</p>
    </div>
  );
}

ContactWidget.propTypes = {
};

export default ContactWidget;
